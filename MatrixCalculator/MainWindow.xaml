<Window x:Class="MatrixCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <Label>Input Matrix</Label>
            <TextBox Height="300" Name="MatrixInputTextbox" TextWrapping="Wrap" AcceptsReturn="True"></TextBox>
            <Button Name="MatrixInverseButton" Click="MatrixInverseButton_Click">Inverse</Button>
            <Button Name="MatrixDeterminantButton" Click="MatrixDeterminantButton_Click">Determinant</Button>
            <Button Name="MatrixCholeskyButton" Click="MatrixCholeskyButton_Click">Cholesky</Button>
        </StackPanel>
        <StackPanel Grid.Row="2">
            <Label>Output</Label>
            <TextBox Name="MatrixOutputTextbox"  Height="300"></TextBox>
        </StackPanel>
    </Grid>
</Window>
